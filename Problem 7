#include <conio.h>
#include <iostream>
using namespace std;
int main()
    {
        int y, sum; 
        
        cout << "input last number" << endl;
        cin >> y;
        
        for ( int number = 1; number <= y; number += x )
        {
           
           sum += number; 
        }
        cout << "The summation from "<<1<<" to "<<y<<" is " << sum << endl; 
        return 0;
    }
